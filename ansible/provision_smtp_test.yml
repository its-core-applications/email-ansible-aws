---

- hosts: localhost
  become: false
  tasks:
    - import_role:
        name: ec2_launch
      vars:
        launch_class: smtp-test
        launch_ami_class: base
        launch_groups:
          - smtp-elb
        launch_tags:
          Status: development

- import_playbook: smtp_test.yml

