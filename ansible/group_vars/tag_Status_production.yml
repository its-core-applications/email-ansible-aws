---
# This is mainly a place to put Vault lookups that isn't all.yml, for now.

sensu_rabbitmq_password: "{{ lookup( 'hashi_vault', 'secret=secret/sensu/rabbitmq token={{ vault_token }}' ) }}"
ops_pager: "{{ lookup('hashi_vault', 'secret=secret/phone/pager token={{ vault_token }}') }}"
