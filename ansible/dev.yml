---

- hosts: Class_dev
  vars:
    bootstrap: true
  tasks:
    - when: region == aws_region
      block:
        - import_role:
            name: base
        - import_role:
            name: build

