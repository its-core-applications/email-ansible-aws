---

- hosts: Status_spundown
  gather_facts: false
  tasks:
    - name: Destroy host
      become: false
      delegate_to: localhost
      ec2:
        region: "{{ aws_region }}"
        instance_id: "{{ instance_id }}"
        state: absent
