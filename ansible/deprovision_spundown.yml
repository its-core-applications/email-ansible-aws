---

- hosts: tag_Class_spundown
  tasks:
    - name: Destroy host
      become: false
      delegate_to: localhost
      ec2:
        region: "{{ aws_region }}"
        instance_id: "{{ ec2_id }}"
        state: absent
