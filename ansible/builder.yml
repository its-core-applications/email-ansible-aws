- hosts: Class_builder
  vars:
    bootstrap: true
  tasks:
    - block:
        - import_role:
            name: base

        - import_role:
            name: build

- hosts: Class_master:&region_us_west_2
  tasks:
    - block:
        - import_role:
            name: s3repo_build
          tags:
            - always
