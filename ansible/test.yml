- hosts: localhost
  become: false
  tasks:
    - include_role:
        name: ec2_nlb
      vars:
        nlb_name: mx-test
        tg_group: Class_mx_test
        tg_vpc_id: "{{ ec2_subnets_vpn[aws_region].0.vpc_id }}"
        nlb_ports:
         - 25
        nlb_dns_name: mx-test.{{ subd }}
