---

- hosts: Class_master:&Status_spinup
  vars:
    bootstrap: true
  tasks:
    - when: region == aws_region
      block:
        - import_role:
            name: base

        - import_role:
            name: master

        - import_role:
            name: vault
