---

- hosts: tag_Class_builder:!tag_Status_spundown
  gather_facts: false
  tasks:
    - name: Set status to spundown
      delegate_to: localhost
      ec2_tag:
        region: "{{ aws_region }}"
        resource: "{{ ec2_id }}"
        tags:
          Status: spundown
