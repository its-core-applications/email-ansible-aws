- hosts: localhost
  become: false
  tasks:
    - import_role:
        name: ec2_launch
      vars:
        launch_class: authn-relay
        launch_groups:
          - smtp
        launch_tags:
          Status: production

- import_playbook: setup_lb_authn_relay.yml
