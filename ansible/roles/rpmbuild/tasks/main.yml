---

- name: Install basic build dependencies
  yum:
    pkg: "{{ item }}"
    state: present
  with_items:
    - git
    - createrepo
    - rpmdevtools
    - autoconf
    - automake
    - libtool
    - setup
    - gcc-c++

- name: Set up rpmbuild tree
  become: false
  command: rpmdev-setuptree creates=~/rpmbuild/RPMS

