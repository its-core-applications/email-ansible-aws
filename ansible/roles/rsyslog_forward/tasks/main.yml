---

- name: Configure rsyslog forwarding
  template:
    dest: /etc/rsyslog.d/omrelp.conf
    src: omrelp.conf.j2
    owner: root
    group: root
    mode: 0644
  notify: Restart rsyslog
  tags: configuration
