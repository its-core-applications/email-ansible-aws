---

- tags:
    - build
  become: false
  block:
    - name: Download {{ build_name }} RPM
      get_url:
        url: "{{ rpm_url }}"
        dest: ~/rpmbuild/RPMS/x86_64/{{ rpm }}
        checksum: "{{ rpm_checksum }}"

