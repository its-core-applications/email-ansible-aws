#!/bin/bash
# Config file for clam-update-client

# base directory
clamdir=/var/lib/clamav

current=${clamdir}/current
statusfile=${clamdir}/reload-status

# SSH key and host
sshkey=/var/lib/clamav/.ssh/id_rsa
update_server=simta@alien.mr.itd.umich.edu

# What signatures should we grab?
rfolder=/var/clamav/signatures/latest

