---

- name: Install graphite-api
  yum:
    pkg: graphite-api
    state: latest
  notify: Restart graphite

- name: Enable graphite-api
  systemd:
    name: graphite-api
    enabled: yes
  notify: Restart graphite
