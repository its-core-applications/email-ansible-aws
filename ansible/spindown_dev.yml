---

- hosts: Class_dev:!Status_spundown
  gather_facts: false
  tasks:
    - name: Set status to spundown
      delegate_to: localhost
      become: false
      ec2_tag:
        resource: "{{ instance_id }}"
        tags:
          Status: spundown
