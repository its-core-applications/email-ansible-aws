---

- hosts: tag_Class_dev
  gather_facts: false
  tasks:
    - name: Set class to spundown
      ec2_tag:
        region: "{{ aws_region }}"
        resource: "{{ ec2_id }}"
        tags:
          Class: spundown
