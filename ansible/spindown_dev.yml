---

- hosts: Class_dev:!Status_spundown
  gather_facts: false
  tasks:
    - name: Set status to spundown
      delegate_to: localhost
      ec2_tag:
        region: "{{ aws_region }}"
        resource: "{{ instance_id }}"
        tags:
          Status: spundown
