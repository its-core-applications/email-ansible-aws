---

- hosts: tag_Class_syslog
  gather_facts: false
  tasks:
    - name: Set status to spundown
      ec2_tag:
        region: "{{ aws_region }}"
        resource: "{{ ec2_id }}"
        tags:
          Status: spundown

