---

- hosts: tag_Class_master
  gather_facts: false
  tasks:
    - name: Set status to spundown
      delegate_to: localhost
      become: false
      ec2_tag:
        region: "{{ aws_region }}"
        resource: "{{ ec2_id }}"
        tags:
          Status: spundown

